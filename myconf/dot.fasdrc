_FASD_MAX=5000
_FASD_BACKENDS="native spotlight viminfo current"
_FASD_SINK="$HOME/.fasd.log"
_FASD_VIMINFO="$HOME/.vim/viminfo"

local shell; for shell in dash mksh zsh; do
  _FASD_SHELL="$(command -v $shell)" && break
done
